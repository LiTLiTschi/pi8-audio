# Stack Exchange / Pi forums: SAP fails on many Pi setups (journal: sap_server_register / Operation not permitted).
# Disabling the SAP plugin avoids that broken path and matches common --noplugin=sap advice.
[Service]
ExecStart=
ExecStart=/usr/libexec/bluetooth/bluetoothd --noplugin=sap
